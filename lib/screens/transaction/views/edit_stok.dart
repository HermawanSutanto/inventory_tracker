import 'package:flutter/material.dart';

class EditStok extends StatelessWidget {
  const EditStok({super.key, required barcode});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
